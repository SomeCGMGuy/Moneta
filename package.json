{
  "name": "moneta-capacitor-test",
  "private": true,
  "version": "2.6.5",
  "scripts": {
    "build:web": "rm -rf dist && mkdir -p dist && cp index.html dist/ && cp -R assets css js dist/",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "8.5.1",
    "@capacitor/app": "8.0.1",
    "@capacitor/core": "8.5.1",
    "@capgo/capacitor-native-biometric": "8.6.7"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.1"
  }
}
